# 버전 기록

버전마다 무엇이 바뀌었고 **왜 바꿨는지**를 남긴다. 대부분은 지인들과 실제로 한 판 돌려보고
나온 것들이라, 이유를 안 적어두면 다음에 같은 자리를 원래대로 되돌리게 된다.

---

## v1.8.4 — 2026-09-10

주소창이 계속 남던 진짜 원인을 찾았다. **`deployment_cert` 지문이 빠져 있었다.**

Play Console의 앱 서명 화면에는 "기존 키"와 "양자 내성 암호화 키" 두 개만 복사 버튼으로
보인다. 그런데 인증서를 내려받아 보니 파일이 **셋**이었고, 그중 **기기에 실제로 배포될 때
쓰이는 `deployment_cert`가 화면에 값이 안 보이는 것**이었다. 화면을 보고 옮겨 적는
방식으로는 영영 못 찾았을 값이다.

증상이 고약했다. 안드로이드 앱링크 검증(설정 → 기본으로 설정)은 **통과**해서
`pizzagame.app`이 켜져 있는데도, 크롬이 따로 하는 TWA 검증만 조용히 실패한다.
두 검증이 별개라는 걸 모르면 "검증됐는데 왜 주소창이 남지"에서 막힌다.

이제 넷을 다 넣었다. 사람이 옮겨 적지 않고 인증서 파일에서 openssl로 직접 뽑았다.

- `F2:EA:2D:…` — **deployment_cert.** 기기에 배포되는 서명. 이게 빠져 있었다
- `5F:AA:85:…` — hybrid_classical
- `83:B6:2C:…` — hybrid_pqc
- `A9:25:58:…` — 업로드 키

---

## v1.8.3 — 2026-09-09

지문을 앱 서명 키로 바로잡았다. v1.8.2에 넣은 것은 **업로드 키** 지문이었다.

Play Console 같은 페이지에 '앱 서명 키 인증서'와 '업로드 키 인증서'가 나란히 있는데,
이름만 다르고 생김새가 같아서 아래쪽 것을 집어 왔다. 실제로 설치되는 앱은 구글의 앱
서명 키로 서명되므로 맞을 리가 없었다. (이걸로도 안 끝났다 — v1.8.4 참조)

---

## v1.8.2 — 2026-09-09

`assetlinks.json`을 처음 올렸다. 이 파일이 없으면 안드로이드가 이 도메인과 앱의 관계를
확인하지 못해서, **앱이 설치형이 아니라 주소창 달린 브라우저로 열린다.**

- `public/.well-known/assetlinks.json` — 패키지 `com.pi22a.pizzart`
- 점으로 시작하는 폴더를 정적 서빙이 막지 않는지 테스트로 못박았다. 여기가 막히면
  증상이 "앱에 주소창이 보인다"로만 나타나서 원인을 찾기 어렵다
- 실제 파일이 이 앱을 가리키는지도 테스트로 확인한다

---

## v1.8.1 — 2026-09-08

개인정보처리방침만 고친 판이다. 기능은 그대로다.

**표와 본문이 서로 다른 말을 하고 있었다.** '게임을 하는 동안 오가는 것' 표에는 그린
그림이 "서버 메모리 · 판이 끝날 때까지"로 적혀 있는데, 바로 아래 '그림은 서버에
남습니다' 절에는 파일로 쌓인다고 적혀 있었다. 같은 문서 안에서 어긋나 있었으니
표만 읽은 사람은 반대로 이해했을 것이다. 구글 플레이의 데이터 보안 양식은 이
문서와 대조되므로, 어긋난 채로 두면 반려 사유이기도 하다.

- 표에서 그림을 따로 뗐다 — "메모리 → **서버 파일**"
- 남는 항목에 **방 코드**가 빠져 있어 채웠다
- 맞힌 사람은 "수만 세며 누구인지는 남지 않는다"고 분명히 했다 (코드상 실제로 숫자다)
- **보관 기한을 두지 않는다**는 사실을 명시했다. 지금까지 어디에도 적혀 있지 않았다

---

## v1.8.0 — 2026-09-03

**혼자 와도 판이 서고, 자리 비운 사람이 판을 붙잡지 않는** 판이다. 불특정 다수가
모이는 자리를 준비하는 첫 걸음이다.


### 아무도 안 그리면 30초에 라운드를 접는다

폰을 내려놓고 딴 데 가는 일이 실제로 생긴다. 그러면 나머지는 **아무것도 안 나오는
화면을 120초 내내** 봤다. 한 획도 없으면 30초에 접는다 — 한 획이라도 그었으면
끝까지 기다린다. 느리게 그리는 사람을 쫓아내면 안 된다.

타이머를 따로 하나 더 뒀다. 화면에 보이는 제한시간과 같이 쓰면 남은 시간 표시가
이 타이머에 맞춰 줄어서, 120초짜리 라운드가 30초처럼 보인다.

### 바로 시작

방 목록의 주인공을 **"바로 시작"** 버튼 하나로 바꿨다. 누르면 기다리는 방에 넣어주고,
없으면 하나 만들어 준다. 사람이 내려야 할 결정이 0이 된다.

"방 만들기 → 이름 짓기 → 사람 오길 기다리기"는 결정이 세 번이고, 그 앞에서 그냥
나가는 사람이 있다. 방 직접 만들기와 목록은 접어두었다 — 없애지는 않았다.

**매칭 대기열은 일부러 안 만들었다.** 상시 접속자가 없는 지금 그걸 넣으면 "매칭 중…"만
5분 보여주다 끝난다. 빈 대기열은 방이 없는 것보다 나쁘다 — 된다고 약속해놓고 안 되기
때문이다. 방 목록에 동시에 두 개 이상 기다리는 방이 보이기 시작하면 그때가 적기다.

### 준비 버튼

방장에게는 시작, 나머지에게는 준비를 **같은 자리**에 보인다. 옆에 `2/4`처럼 몇 명이
준비했는지만 붙는다. 방장은 처음부터 준비된 것으로 센다 — 방장에게는 준비 버튼 대신
시작 버튼이 있고, 그걸 누르는 것이 곧 준비의 표시다.

관전자와 끊긴 사람은 세지 않는다. 안 그리고 안 맞히는 사람이나 폰을 껐다 켠 유령
하나 때문에 시작이 영영 막히면 안 된다.

#### 이건 화면에서만 막는다

시작 버튼을 잠그는 것으로 끝내고 **서버에서는 안 막는다.** 색 팔레트는 서버가 막았는데
왜 다른가 — 색은 서버가 쥔 그림 데이터를 바꾸는 일이라 뚫리면 **남들이 보는 판**이
망가진다. 반면 시작은 원래 방장의 권한이고, 뚫어봐야 자기 방을 자기가 일찍 시작하는
것뿐이다.

서버에서 막아봤다가 되돌렸다. 준비 안 한 사람이 하나라도 있으면 start가 조용히
실패하는데, 그러면 "판이 끝날 때까지" 도는 시험이 **실패가 아니라 무한 대기**가 된다.
앞으로 사람을 넣는 시험을 쓸 때마다 밟을 지뢰를 심는 셈이었다.

---

## v1.7.6 — 2026-09-02

욕설을 거르기 시작한 판이다.

### 채팅은 가리고, 이름·방 제목은 받지 않는다

24시간 열린 주소가 되면서 아무나 들어온다. 채팅까지 열어둔 이상 이 자리는 언젠가
반드시 온다.

**둘을 다르게 다루는 이유는 글자가 화면에 머무는 시간이 다르기 때문이다.** 채팅 한
줄은 흘러가지만 이름은 판이 끝날 때까지 결과 화면과 이야기판에 계속 남는다. 방 제목은
방 목록에 걸려 그 방에 들어가지도 않은 사람 눈에까지 든다. 오래 머무는 글자일수록 문을
좁게 잡는다 — 채팅은 별표로 가리고, 이름과 방 제목은 아예 안 받는다.

### 라이브러리를 안 쓴 이유

후보 둘을 이 게임의 문장으로 실제로 돌려봤다. 하나는 **"강아지 새끼 그린거야"를
욕설로** 잡았고, 다른 하나는 정상 낱말 **"시발점"을 별표로** 만들었다. 이 게임은 그림을
설명해서 맞히는 게임이라 대화가 곧 게임이고, 주제에 동물이 있어 "새끼"가 정상 대화에
자주 나온다. **오탐이 게임을 망친다.** 목록과 예외를 우리가 쥐고 있어야 그런 자리를
그때그때 풀 수 있다.

만들면서 그 자리를 그대로 밟았다. 목록에 `새끼야`를 넣었더니 "강아지 새끼야"가 걸려
빼야 했다(`개새끼`는 그대로 잡힌다). `보지`·`자지`는 넣되 "보지 못했어"·"자지 않고"가
살아남도록 예외를 뒀다.

### 우회 표기

한글·자모·영문만 남기고 숫자·기호·공백을 버린 뒤 비교한다. 그래서 `시1발`·`시^발`·
`시 발`·`ㅅㅂ`가 전부 같은 말이 된다. 가릴 때는 **사이에 끼워 넣은 기호까지 함께**
덮는다 — `시1발`을 `*1*`로 두면 무슨 말이었는지 그대로 읽힌다.

### 이름이 막히면 이름 화면으로 되돌린다

저장된 이름으로 자동 입장하는 구조라, 알림창만 띄우고 끝내면 **방에도 못 들어간 채 빈
화면에 갇힌다.** 예전 이름이 목록에 걸리는 사람이 실제로 그렇게 된다.

### 비용은 재봤다

한 번 검사에 **7.9마이크로초**, 초당 12만 건이다. 채팅은 한 사람당 초당 3건 제한이고
결과 화면에서만 열리므로, 9명짜리 방 100개가 쉬지 않고 떠들어도 초당 21밀리초다.
512MB 한 대가 감당한다.

목록은 `content/badwords.json` 한 파일이다. 넣고 빼는 데 코드를 고칠 일이 없다.

---

## v1.7.5 — 2026-09-02

친구들이 한 판 돌리고 나온 화면 이야기를 모아 고친 판이다. 규칙은 그대로다.

### "주제가 안 보인다"

여러 사람에게서 나온 말이다. 원인은 크기보다 **표기가 화면마다 달랐던 것**이었다 —
추론 화면은 `동물`, 그리는 화면은 `주제 동물`, 대기 화면도 `주제 동물`. 같은 것을
세 가지 모양으로 부르면 눈이 매번 새로 찾는다. 전부 **`주제 : 동물`** 하나로 맞추고,
추론 화면의 주제는 17px → 24px로 키웠다.

옆에 있던 "무엇을 그린 걸까요"는 뺐다. 주제와 크기가 비슷해 **주제를 묻어버렸고**,
바로 아래가 답 칸이라 무엇을 하는 화면인지는 이미 분명하다.

### 점수는 숫자만

"지금 맞히면 10점"에서 **"10점"** 으로 줄였다. 매 회차 같은 자리에서 반복되는 말이라
몇 번 보고 나면 읽히지 않으면서 줄만 길어진다. 바뀌는 것은 숫자뿐이다.

알약이 `<p>`라 줄 전체로 늘어나 있던 것도 글자 폭까지만 차지하게 고쳤다. 두세 글자에
화면 폭을 다 쓸 이유가 없다.

### 그리는 동안 남은 시간이 아무 데도 없었다

**상단바를 걷어내면서 시간이 같이 사라진 자리다.** 추론 단계는 회전 안내 원 안에 숫자가
들어가 살아남았는데, 그리기 단계는 그리는 쪽도 기다리는 쪽도 언제 끝나는지 알 방법이
없었다. 120초부터 줄어드는 숫자를 두 화면에 같이 적는다 — 그리는 사람은 손을 서두를지,
기다리는 사람은 낙서를 접을지 정하려면 둘 다 알아야 한다.

### 조립판에서 첫 조각 강조를 걷어냈다

내가 받았던 조각 하나만 주황으로, 그것도 3배 굵게 칠하고 있었다. **이 화면의 주인공은
완성된 그림**이지 누가 어느 조각을 봤느냐가 아닌데, 굵은 주황 부채꼴이 그림 위에 얹히면
그림보다 그 선이 먼저 읽힌다.

조각 경계선 자체는 남긴다 — 피자처럼 잘렸다는 것이 이 게임의 정체성이라, 그것까지
지우면 그냥 동그란 그림이 된다.

---

## v1.7.4 — 2026-09-02

친구들과 한 판 돌리고 나온 화면 손질 세 가지다. 규칙은 그대로다.

### 컬러 버튼도 켜지면 주황

**컬러판인데 버튼이 켜져 보이지 않았다.** 글자만 무지개로 칠했었는데, 이 화면에서
주제 버튼도 지우개도 전부 "켜지면 주황"이라 **이 버튼만 규칙이 달랐다.** 같은 화면에서
같은 뜻은 같은 모양이어야 한다. 버튼 자체를 주황으로 칠하고 글자는 어두운 색으로 뒤집었다.

무지개는 아깝지만, 주황 배경 위의 무지개 글자는 서로 싸운다.

### '정원'을 '인원'으로

몇 명까지 받는 방인지를 묻는 말로 '정원'은 딱딱했다. 범위도 `3~9명` 대신
**최소 3명 / 최대 9명**으로 풀어 적는다 — 스테퍼 옆 숫자가 지금 값이라, 범위까지
같은 모양으로 붙어 있으면 어느 쪽이 현재 값인지 헷갈린다.

안내에서 "줄이면 그 위로는 새로 못 들어옵니다"는 뺐다. 스테퍼를 만져보면 바로 보이는
것을 굳이 적어 줄을 길게 만들 이유가 없다. 대신 **더 줄일 수 없을 때는 그 이유를**
여전히 적는다("지금 4명이 있어 더 줄일 수 없습니다") — 그건 만져봐도 알 수 없다.

---

## v1.7.3 — 2026-09-02

친구들에게 링크를 돌린 첫날 바로 나온 것 하나를 고친 판이다.

### 주소창에 넣은 긴 방 코드가 화면을 밀어냈다

친구가 `?room=` 에 **60자짜리 문자열을 넣고** 들어왔더니, 그 코드가 방 이름 옆에
그대로 찍혀 **상단 줄이 세 줄로 부풀고 화면이 밀렸다.** 폰에서는 게임 화면이 한참
아래로 내려갔다.

두 겹으로 막았다.

**들어오는 문에서 코드를 손질한다.** 12자로 자르고, 눈에 안 보이면서 자리만 차지하거나
줄을 바꾸는 글자(공백·제어문자·제로폭)를 걷어낸다. **화면만 고치지 않은 이유**는 코드가
화면에만 쓰이는 값이 아니기 때문이다 — 방을 찾는 열쇠이고, 자리 지도(`seats`)의 열쇠이고,
링크에 실려 나가는 값이다. 문에서 막지 않으면 다음에 다른 자리에서 또 샌다.

자르는 규칙은 **결정적**이다. 아니면 같은 링크를 받은 두 사람이 서로 다른 방에 떨어져,
"같은 주소인데 서로 안 보인다"는 가장 찾기 어려운 종류의 문제가 된다.

문자 자체는 넓게 받는다. `?room=피자` 처럼 원하는 말을 코드로 쓰는 것은 이 게임이
권해온 사용법이라 한글도 그대로 둔다.

**화면에서도 한 줄로 접는다.** 코드를 잘라도 방 이름까지 합치면 여전히 길어질 수 있고,
그때 이 줄이 부풀면 화면이 통째로 밀린다. 넘치는 만큼은 …으로 접는다.

---

## v1.7.2 — 2026-09-02

셋이서도 판을 열 수 있게 하고, 방 정원을 방장 손에 쥐여준 판이다.

### 최소 인원을 3명으로

**넷을 못 채워 판을 못 여는 쪽이, 셋으로 도는 것보다 아쉬웠다.** 셋이면 출제자 하나에
맞히는 사람 둘이다.

바꾸기 전에 조각이 커지지 않는지부터 확인했다. 조각 수는 사람 수가 아니라
`sliceCountMin`(8)이 바닥이라(`slicer.ts`의 `sliceCount`), **셋이어도 한 장은 여전히
8분의 1이다.** 사람이 적다고 조각이 커져서 쉬워지는 일은 없다.

그런데 유니티 이식 문서에는 최소 인원이 4인 이유가 *"3명이면 조각이 3장이라 한 장이
원본의 3분의 1이다. 너무 쉽다"* 로 적혀 있었다. **`sliceCountMin`이 생기기 전의
이야기라 지금은 성립하지 않는다.** 이식하는 사람이 그대로 믿으면 잘못 만들 자리라
그 줄을 고쳐 적었다.

### 방 정원을 방장이 정한다

로비에서 **3~9명** 사이로 고른다. 버튼을 3부터 9까지 늘어놓지 않았다 — 그러면 로비가
버튼밭이 된다. **지금 값 하나만 크게 보여주고 양옆에서 한 칸씩 옮기는 스테퍼**로 뒀다.

**지금 있는 사람 수 아래로는 못 내린다.** 내릴 수 있게 하면 "정원 4명"이라고 적힌 방에
여섯 명이 앉아 있게 되고, 그렇다고 남는 사람을 자동으로 내보내면 방장이 누른 적 없는
강퇴가 일어난다. 내보내는 것은 강퇴 버튼이 할 일이다.

위로는 규칙의 상한(`maxPlayers`)까지다. 상한은 조각 나누기와 라운드 수가 감당하는
범위라 방장이 넘길 수 있는 값이 아니다. 서버가 같은 판정을 다시 하므로 화면의 잠금은
안내일 뿐이다.

#### 곁들여 고친 것

스테퍼가 **서버 응답을 기다리면 연타가 통째로 먹히지 않았다.** 응답 전의 클릭은 전부
같은 값을 보내게 되어, 여덟 번을 눌러도 한 칸만 움직인다. 먼저 옮기고 서버 응답이
덮게 했다.

`fake-player`의 시작 조건에 **4가 박혀 있었다.** 서버가 알려준 `minPlayers`를 쓰게
바꿨다 — 숫자를 박아두면 규칙이 아니라 도구 때문에 판이 안 도는 일이 생긴다.

---

## v1.7.1 — 2026-09-02

밝은 모드를 쓸 만하게 만들고, 배포를 한 명령으로 줄인 판이다. 게임 규칙은 그대로다.


### 테마가 기기 설정을 따라간다

**고른 적이 없으면 기기 설정을 따른다.** 폰을 밝은 모드로 쓰는 사람에게 어두운 화면을
들이밀 이유가 없다. 한 번 누르면 그때부터는 고른 값이 이긴다 — 기기와 다르게 쓰고 싶은
사람의 뜻이 더 분명하기 때문이다. 고른 뒤에는 기기 설정이 바뀌어도 안 흔들린다.

바로 위의 원판 테두리 문제를 먼저 고쳤기에 넣을 수 있었다. 안 고치고 이걸 넣었으면
**밝은 모드를 쓰는 사람만 원판이 묻힌 화면을 받게** 됐을 것이다.

#### 곁들여 고친 것

테마만 `localStorage`를 날것으로 읽고 있었다. 그 줄이 모듈 맨 위에 있어서, 사생활 보호
모드처럼 **읽기만 해도 예외가 나는 환경에서는 게임이 통째로 안 떴다.** 다른 값들이 쓰던
try/catch 래퍼를 위로 올려 같이 쓰게 했다. 기억은 편의지 게임의 조건이 아니다.

### 밝은 모드에서 원판이 배경에 묻히던 것

**밝은 모드를 기본으로 할까**를 따져보다가 발견했다. 밝은 모드의 배경(`#e9e0cf`)과
반죽(`#f6efe2`)이 거의 같은 크림색이라, **이 게임의 주인공인 원판이 배경에서 떠오르는
정도가 1.1:1**이었다. 어두운 모드에서는 15.5:1이다.

테두리(크러스트)마저 밝은 모드에서 1.25:1이라 윤곽조차 안 잡혔다. 테두리 색이 테마를
타도록 고쳤다 — 조각판과 조립판은 이미 그렇게 하고 있었고(`reveal.ts`, `slice-view.ts`)
**그리는 캔버스만 빠져 있었다.** 그림 모아 보기의 원판에는 테두리가 아예 없어서 같이 넣었다.

기본값은 **어두운 모드 그대로** 둔다. 이 게임의 주인공이 크림색 원판이라 어두운 배경에서
가장 잘 떠오른다. 이걸 고쳐두면 나중에 "시스템 설정 따라가기"도 안전해진다 — 지금까지는
그렇게 하면 밝은 모드를 쓰는 사람만 묻힌 화면을 보게 됐다.

---

## v1.7.0 — 2026-09-02

이름을 **PIZZART(피자르트)** 로 바꾸고, 24시간 서버로 옮긴 판이다.

### 이름을 PIZZART로

`PIZZA`는 너무 일반적이라 검색도 안 되고 상표도 못 낸다. `PIZZART`는 피자와 아트를
합친 것이라 **게임 내용(그림)까지 드러나고 구별력이 있다.**

저장소 이름, 맥의 폴더 이름, 서버의 폴더와 서비스 이름까지 전부 맞췄다.

**도메인(`pizzagame.app`)은 그대로 둔다.** 도메인은 이름을 바꾸는 것이 아니라 새로
사는 것인데, `pizzart.app`과 `pizzart.com`은 이미 남의 것이다. 바꿀 거라면 **TWA를
빌드하기 전**이 마지막 기회다 — 그 뒤에는 앱이 도메인에 묶여서 재빌드·재심사에
설치한 사용자까지 흔들린다.

치환할 때 `pizzagame.app` 안의 `pizza`가 휩쓸리지 않게 먼저 감췄다가 되돌렸다.
TWA 패키지 이름도 도메인을 거꾸로 쓰는 것이라 `app.pizzagame.twa` 그대로다.

화면 워드마크에서는 수동 공백을 뺐다. 일곱 글자라 공백까지 넣으면 폰에서 줄이
넘친다 — 자간은 CSS가 맡는다.


### 서버가 스스로를 지키게 했다

24시간 고정 주소로 나가기 전에, **아무 메시지나 던져도 죽지 않는지** 훑었다.
지금까지는 임시 터널로 지인들만 들어왔지만 곧 주소를 아는 누구나 브라우저 콘솔로
아무거나 보낼 수 있게 된다.

이 게임에서 서버가 죽는 것은 특히 비싸다 — **방 상태가 메모리에만 있어서 예외 하나로
프로세스가 내려가면 그 순간 돌던 판이 전부 날아간다.**

메시지 스물세 종류에 고약한 값 스물몇 가지를 꽂아 1,500번쯤 던져봤다. 결과:

- **네트워크 경로는 이미 막혀 있었다.** `index.ts`가 형태를 검사하고 넘긴다.
  실서버에 직접 쏴서 확인했고 멀쩡했다
- 다만 **`Session` 자체는 부르는 쪽을 믿고 있었다.** 검사를 안 거치는 길이 하나라도
  생기면(새 메시지 경로, 다른 이식) 그대로 터진다. 세 자리를 막았다
  - `addStroke` / `eraseInk` — 배열이기만 하고 안이 `[null]` 같은 것
  - `handle` — 메시지 자체가 객체가 아닌 것
  - `chat` — `String(text)`가 남의 `toString`을 부르던 것

값 하나가 이상한 것과 판이 끝나는 것은 치러야 할 대가가 너무 다르다. 그래서
"막았는가"가 아니라 **"죽지 않는가"** 를 기준으로 잡았다 — 이상한 요청은 조용히
무시되고 판은 계속 돈다.


---

## v1.6.0 — 2026-09-02

색과 지우개가 들어가고, **웹이 메인 제품이 된** 판이다. 유니티판을 위한 규칙 실험장이
아니라 그 자체로 출시할 것이 됐고, 그에 맞춰 배포 준비가 시작됐다 —
도메인(`pizzagame.app`), PWA, 프로덕션 서빙까지.


### 판이 도는 동안 화면이 안 꺼진다

그리는 시간이 120초인데, **기다리는 사람은 손을 안 대므로** 폰이 화면을 꺼버린다.
다시 켜면 회차가 넘어가 있고 답을 낼 기회를 통째로 놓친다. 판이 도는 동안
(로비가 아닌 동안) 화면을 붙잡아 둔다.

브라우저가 순순히 내주지 않는다는 점이 중요하다. **사용자가 화면을 안 보고 있으면
브라우저가 알아서 놓아버리므로**, 돌아왔을 때 다시 잡아야 한다. 되는 기기와 안 되는
기기도 갈리는데, 안 되면 그냥 안 되는 것이고 판은 그대로 돈다 — 화면이 안 꺼지는 것은
편의지 게임의 조건이 아니다.

### 뒤로가기는 손대지 않았다

TWA에서 안드로이드 뒤로가기를 처리해야 하나 확인해 봤더니, **이미 자연스럽게 동작한다.**
방을 오갈 때 주소를 바꾸는 방식이라(`location.href`) 방문 기록이 그대로 쌓여서,
방에서 뒤로가기를 누르면 로비로 가고 로비에서 한 번 더 누르면 앱이 닫힌다.
따로 넣을 것이 없었다.


### 홈 화면에 깔 수 있게 됐다 (PWA)

`manifest.webmanifest`, 서비스 워커, 아이콘을 넣었다. 이제 폰에서 **홈 화면에 추가**하면
주소창 없이 앱처럼 뜬다. 나중에 Play에 TWA로 올릴 때도 이 셋이 전제 조건이다.

**아이콘은 스크립트로 만든다** (`npm run icons`). 소재는 게임의 정체 그대로 — 8조각으로
잘린 원에 내 조각 하나만 소스색이다. 이미지 라이브러리를 안 쓰고 zlib(노드 기본 내장)
위에 PNG를 직접 썼다. 아이콘 넉 장 만들자고 의존성을 들이면 런타임 의존성이 `ws`
하나뿐이라는 장점이 사라진다.

**서비스 워커는 오프라인 플레이용이 아니다.** 이 게임은 혼자 할 수 있는 것이 없다.
워커를 두는 이유는 (1) 없으면 설치가 안 되고 (2) TWA는 주소창이 없어서 인터넷이
끊기면 앱 안에 브라우저 오류 페이지가 통째로 뜨기 때문이다.

전략은 **network-first**다. 캐시를 먼저 보지 않는다 — 서버가 규칙을 쥐고 있어서 오래된
클라이언트가 캐시에서 살아나면 새 서버와 메시지 형식이 어긋날 수 있고, 그러면 원인
찾기가 아주 어려워진다.

#### 고치면서 잡은 것

처음엔 연결이 끊겼을 때 캐시를 뒤져 **게임 껍데기**를 돌려줬다. 실제로 서버를 내리고
확인해 보니 **완전히 빈 페이지**가 됐다 — 화면을 켜는 것은 서버가 보내는 방 상태인데
그게 안 오니 아무 화면도 안 켜진 것이다. 화면을 여는 요청에는 캐시 대신 안내 화면을
주도록 바꿨다.


### 어두운 모드를 중성 회색으로

갈색 계열이던 어두운 모드를 회색으로 바꿨다.

```
전체 배경        #1c1712 → #181818
안 고른 버튼      #2b2118 → #3a3a3a
안 고른 버튼 테두리 #3d3227 → #555555
```

같이 옮긴 것 두 가지가 있다. **입력칸 배경**(#241d16 → #242424)은 안 바꾸면 회색
화면에 갈색 칸만 남는다. **조각판의 아직 못 본 칸**(#241d16 → #242424)도 마찬가지인데,
이 칸은 애초에 "배경에 눕혀 두는 것"이 목적이라 배경이 바뀌면 따라와야 한다.

피자 자체(반죽·잉크·소스·크러스트)는 그대로다. 어느 배경에서도 종이처럼 읽혀야 하는
색이라 테마를 안 탄다. 밝은 모드도 손대지 않았다. 내보내는 공유 그림의 색도 그대로다 —
그건 받는 사람의 화면에서 열리므로 이쪽 테마와 무관해야 한다.


### 흑백판과 컬러판을 방마다 고른다

**컬러로 그리면 너무 쉬워진다**는 의견이 나왔다. 조각 하나만 봐도 "빨갛고 둥근 것"으로
좁혀지기 때문이다. 어느 한쪽이 옳다고 정하는 대신 **로비에서 방마다 고르게** 했다.
같은 사람들이라도 판마다 난이도를 바꿔 놀 수 있다.

- **흑백** — 검은색으로만 그린다. 지금까지의 판과 같다.
- **컬러** — 18색을 쓴다. 버튼 글자가 무지개로 칠해져서 어느 판인지 글자만 봐도 안다.

기본값은 **흑백**이다. 지금까지 쌓인 점수 설계(10점에서 회차마다 1점씩)가 전부 흑백
기준으로 맞춰진 값이라, 컬러는 골라서 켜는 쪽이 맞다.

**규칙은 서버가 지킨다.** 흑백판이면 무슨 색을 보내든 `addStroke`에서 검정으로 눌러버린다.
화면에서 팔레트를 감추는 것만으로는 안 된다 — 그건 안 보이게 한 것이지 못 하게 한 것이
아니다. 화면이 하는 일은 헷갈리지 않게 하는 것뿐이다(흑백판인데 팔레트가 열려 있으면
빨강을 골라 그어놓고 검게 나오는 것을 보게 된다).

**판이 도는 중에는 못 바꾼다.** 중간에 바뀌면 앞 라운드는 컬러로, 뒤 라운드는 흑백으로
그려져 점수를 견줄 수 없게 된다. 한 판이 끝나 로비로 돌아오면 그때 바꾼다.

낙서판은 늘 컬러다. 점수와 무관하고, 거기서 색은 "누가 그렸나"를 나르기 때문이다.

### 그리는 시간 90초 → 120초

색과 지우개가 생기면서 90초가 빠듯해졌다.


### 공유하면 같은 그림이 두 장 갔던 것

**카카오톡 전송창에 pizzart.png가 두 장 떴다.** 만드는 쪽은 처음부터 한 장만 보내고 있었다
(눌린 횟수와 실린 장수를 세는 테스트로 확인했다). 문제는 **확인한 것과 보낸 것이
달랐다**는 데 있었다.

```
보낼 수 있는지 물어볼 때:  { files }
정작 보낼 때:              { files, text }
```

받는 앱은 그림과 글자를 **각각 한 덩이로 세는 수가 있다.** 그래서 한 장을 보냈는데
전송창에는 두 칸이 생겼다. 이제 물어보는 꾸러미와 보내는 꾸러미가 정확히 같다 —
그림만 보낸다. 글자는 어차피 그림 안에 다 들어 있다.

공유창이 떠 있는 동안 두 번째를 안 띄우는 잠금도 한 겹 더 뒀다.

### 색 18가지와 지우개

**출제자도 색을 쓴다.** 지금까지는 검정 한 색이었다. 팔레트는 3줄 × 6칸으로, 첫 줄이
무채·자연색, 둘째 줄이 따뜻한 쪽(빨강~청록), 셋째 줄이 찬 쪽(하늘~분홍)이라 손이
자리를 기억한다.

색 개수를 18로 정한 근거: "한 번에 구분할 수 있는 색은 8~12개가 한계"라는 이야기가
흔한데, 그건 **색이 뜻을 나르는 경우**(범례를 외워야 하는 경우)의 한계다. 그림 팔레트는
그 문제를 안 겪는다 — 사과니까 빨강을 고르는 것이라 외울 것이 없다. 다만 **낙서판의
색은 "누가 그렸나"를 나르므로** 그 한계가 그대로 적용돼서, 낙서판은 팔레트만 같이 쓰고
처음 배정되는 색은 앞에서부터 서로 다르게 준다.

흰색은 뺐다. 캔버스가 크림색이라 흰색으로 그으면 안 보이고, 사람들은 그걸 고장으로 읽는다.

색은 **획에 붙어 다닌다.** 그림 단위로 들고 있으면 조각내기가 획을 여러 토막으로 쪼갤 때
그 토막들이 무슨 색이었는지 잃는다. 서버는 팔레트에 있는 색인지 확인한다 — 안 그러면
배경과 같은 색으로 그어 "안 보이는 그림"을 만들 수 있고, 그러면 아무도 못 맞힌다.

**지우개는 지나간 자리만 지운다** — 그림판과 같다. 처음에는 획 지우개로 만들었다.
문 획 하나가 통째로 사라지는 방식인데, "픽셀 지우개는 안 된다"는 판단에서 나온 것이었다.
그 판단 자체는 지금도 맞다 — 조각내기가 폴리라인 클리핑 위에 서 있어서 그림을 픽셀로
바꾸는 순간 이 게임의 핵심 장치가 통째로 무너진다. 다만 **그 걱정이 막으려던 것은
픽셀이지 부분이 아니었다.** 폴리라인을 폴리라인으로 자르면 자료 구조는 그대로다.

바꾼 이유는 유니티판(스팀)을 만들면서 나왔다. 마우스는 손가락보다 획이 길고 떨려서,
큰 원을 한 번에 그은 뒤 한 군데만 고치고 싶은 일이 잦다. 획 지우개는 그때 통째로 다시
그리게 만든다. 두 판의 지우는 느낌이 다르면 규칙 실험장으로서의 값어치가 떨어져서
웹판도 맞췄다.

**원이 아니라 캡슐로 자른다.** 포인터 이벤트가 오는 자리마다 원으로만 지우면 빨리
문질렀을 때 샘플 사이가 안 지워져 얼룩이 남는다. 지나온 자리를 선분으로 이어야 한다.

프로토콜도 같이 바뀌었다. 획 번호(`{t:'erase', index}`)로는 표현이 안 된다 — 획 하나가
여러 토막으로 쪼개지기 때문이다. 그렇다고 지운 결과를 통째로 보내면 클라이언트가 그림을
마음대로 갈아치울 수 있으니, **지나간 자리(`path`)만 보내고 자르는 것은 서버가 한다.**
경로는 획과 같이 50ms마다 묶어 보낸다 — 포인터가 움직일 때마다 보내면 초당 메시지
예산(40)에 걸려 조용히 버려지고, 서버가 못 받은 자리의 잉크가 되살아난다. 묶음을 끊을
때는 마지막 점을 다음 묶음의 첫 점으로 남긴다. 안 그러면 묶음 경계의 한 구간이 아무에게도
안 지워진다.

낙서판에도 같은 지우개를 넣었고, 거기서는 내가 그은 획만 물린다.

### 낙서판에서 색이 뒤늦게 입혀지던 것

**긋는 동안은 엉뚱한 색으로 보이다가 손을 떼는 순간 고른 색으로 바뀌었다.**

그리는 중인 획을 화면에 얹을 때 색을 안 붙이고 있었다. 그러면 그리는 쪽 코드가
`색 ?? id에서 뽑은 기본색`으로 떨어져서, 긋는 동안에는 **기본색**으로 그려졌다.
손을 떼야 비로소 고른 색이 붙은 획으로 저장되면서 다시 칠해졌다. 서버 왕복과는
아무 상관이 없었다 — 내 화면 안에서만 일어나던 일이다.


### 그림은 판이 끝난 뒤 한자리에 모아 저장한다

회차마다 결과 화면에 있던 '저장하기'를 없애고, **최종 결과 화면에 그 판의 그림을 전부
모아 보여주는 자리**를 만들었다. 저장 버튼도 거기 하나만 둔다.

회차마다 두는 것이 나빴던 이유는 두 가지다. 결과를 보다 말고 저장하러 흐름이 끊기고,
아홉 장이 **아홉 개의 파일로 흩어져** 아무도 다시 안 본다. 한 판은 한 장으로 남아야 한다 —
그림 아홉 개와 최종 순위가 한 장에 같이 들어간다.

그림은 **서버가 `final`에 실어 보낸다.** 클라이언트가 회차마다 모아두는 방법도 있었지만
그러면 도중에 들어왔거나 새로고침한 사람만 텅 빈 화면을 본다. 실제로 모인 그림으로 재보니
한 장 평균 16KB라 아홉 장을 합쳐도 150KB 남짓이고, 판이 끝날 때 한 번 가는 것이라
감당할 만하다. 그림 밑의 이름은 **id가 아니라 이름으로 박아 보낸다** — 판이 끝나고
나간 사람도 "누가 그렸는지"는 남아야 한다.

빈 원판은 모으지 않는다. 아무도 안 그리고 넘어간 라운드는 보여줄 것이 없다.

### 전통문화를 뺐다

주제 9개, 단어 450개가 됐다. 파일만 지웠으므로 되돌리려면 `09-tradition.json`을
다시 넣으면 된다 — 번호 09 자리는 비워뒀다.

---

## v1.5.1 — 2026-08-27

버그 하나만 고친 판이다. 모바일에서 제보가 왔지만 기기 문제가 아니었다.

### 맞힌 뒤에도 답을 낼 수 있던 것

**맞혔다고 표시가 떠 있는데 입력칸과 제출·스킵 버튼이 전부 열려 있었다.** 모바일에서
발견됐지만 기기 문제가 아니다 — 어디서든 났다.

잠금을 **두 군데서 각자 정하고 있었다.** 방 상태가 올 때 잠그고, 조각이 올 때
`setSpectating(false)`로 통째로 풀었다. 그런데 **맞힌 사람도 회차마다 조각을 계속 받는다**
(`session.endAttempt`가 `seen`에 있는 사람 전부에게 보낸다). 그래서 회차가 넘어갈 때마다
잠금이 그 자리에서 풀렸다.

조각을 안 보내는 쪽으로 고칠 수도 있었지만, 맞힌 사람도 자기 조각은 계속 봐야 한다.
대신 **잠금을 정하는 곳을 하나로 모았다**(`paintAnswerLock`). 두 군데서 각자 정하면
나중에 오는 쪽이 앞의 결정을 조용히 덮는다.

스킵도 같은 구멍으로 풀리고 있었다. 함께 막혔다.

---

## v1.5.0 — 2026-08-27

**작은 판이다.** 기능을 늘리기보다 **끊겼을 때 돌아올 길**을 만들고, 이 게임이
**밖으로 나갈 통로**를 하나 뚫었다.

둘 다 처음부터 비어 있던 자리였다. 재연결은 첫 판(v1.0.0-beta)부터 `net.ts`에
*"다음 단계 작업이다"*라고 적어두고 지나온 것이고, 공유는 판이 끝나면 아무것도
안 남는다는 문제였다.

### 끊겨도 스스로 다시 붙는다

지금까지는 끊기면 **새로고침 말고는 돌아올 길이 없었다.** 자리는 브라우저에 남아 있어서
점수도 그대로인데, 그걸 되찾는 문을 안 만들어뒀다. 지하철에서 폰으로 하면 흔한 일이다.

서버는 이미 다 준비돼 있었다. 같은 `cid`로 `join`을 다시 보내면 원래 자리에 앉히고
그 라운드에 준 것을 전부 다시 보내준다(`restore`). **막혀 있던 것은 그 join을 다시
보낼 사람이 없다는 것뿐이었다.**

0.5초부터 두 배씩 늘려 10초까지, 끝없이 시도한다. 잠깐 끊긴 사람에게
"새로고침하세요"라고 하는 것이 제일 나쁘다.

세 가지를 같이 처리했다.

- **밀려 있던 것은 버린다.** 끊긴 사이에 친 답이 되살아나면 이미 지난 회차의 답으로
  채점된다. 획도 마찬가지다 — 그 사이 라운드가 넘어갔으면 남의 그림에 선이 그어진다.
- **강퇴당하면 다시 두드리지 않는다.** 안 그러면 내보내진 사람이 자동으로 문을 계속 두드린다.
- **로비에서는 방 목록만 다시 받는다.** 돌아갈 자리가 없기 때문이다.

### 결과를 그림으로 내보낸다

이 판이 밖으로 나가는 유일한 통로다. 결과 화면의 **`그림으로 저장`**을 누르면
정답·원본 그림·다들 뭐라고 답했는지가 한 장으로 나온다.

최종 순위는 일부러 안 넣었다. 숫자 몇 줄이라 남에게 보여줄 것이 못 된다.
남는 것은 정답이 뜨는 그 순간 — "아 그게 낙타였어?"가 나오는 화면이다.

폰에서는 공유창이 바로 뜨고(카톡·인스타), 안 되면 내려받는다. 서버는 아무것도 안 한다 —
그림도 답도 이미 화면이 들고 있는 값이라 보이지 않는 캔버스에 다시 그리기만 하면 된다.

**버튼 글자가 기기마다 다르다.** 공유창이 되는 곳에서는 `공유하기`, 안 되는 곳에서는
`그림으로 저장`. 폰에서 '저장'이라고 적어두면 공유창이 뜨는 것이 놀랍고, PC에서
'공유하기'라고 적어두면 파일이 내려와서 또 놀란다. 기기 이름으로 짐작하지 않고
가짜 파일 하나로 **직접 물어본다** — 그쪽이 안 틀린다.

**누를 때만 열린다.** 결과 화면이 뜰 때는 아무 일도 안 일어난다. 애초에 공유창은
사람이 방금 누른 직후에만 열리도록 브라우저가 막아두고 있다.

**서버에 올려 링크로 주는 길은 일부러 안 갔다.** 남이 그린 그림이 영구 주소로 남으면
신고를 받아줄 사람이 있어야 한다. 이미지 한 장이면 카톡으로 충분하다.

### 고친 것

- **관전자가 명단 중간에 끼던 것.** 인원 알약 줄은 사실상 출제 순번표인데,
  안 그리는 사람이 중간에 있으면 "다음이 누구지"를 세는 데 방해가 된다. 맨 뒤로 보냈다.

---

## v1.4.0 — 2026-08-27

**방을 여러 개 열 수 있게 된 판**이다. 그전까지는 방 코드를 정해서 링크에 붙여 나눠줘야
했는데, 이제 주소 하나로 로비에 모여 각자 방을 만들고 고른다. 그 위에 방장이 방을
관리할 수단(강퇴·잠금)이 붙었다.

제시어도 210개에서 500개로 늘렸다. 테스터들이 50판 가까이 돌면서 실제로 바닥냈기 때문이다.

### 로비가 진짜 로비가 됐다

예전에는 주소에 `?room=`이 없으면 `LOBBY`라는 **방 하나**로 보냈다. 그래서 로비가 말만
로비지 다른 방과 구별이 안 됐고, 친구를 부를 때마다 방 코드를 정해서 알려줘야 했다.

이제 **터널 주소를 그대로 주면 된다.**

```
주소 열기 → 이름 한 번 → 로비(방 목록) → 방 만들기 / 골라 들어가기
```

- 이름은 브라우저에 남아서 두 번째부터는 안 묻는다.
- 방 목록은 **웹소켓으로 실시간**이다. 누가 방을 만들면 새로고침 없이 뜬다.
- 게임 중인 방도 보여준다. 관전이 있는 게임이라 들어갈 데가 있고, 감추면 친구가 어느 방에
  있는지 알 방법이 없다. 대신 버튼에 **`관전으로 입장`**이라고 적는다.
- 방 이름은 **`○○의 방`**이 기본이고 직접 지을 수도 있다.
- **새로고침 버튼**이 있다. 목록은 원래 저절로 오지만, 소켓이 끊기면 그 길이 막히고
  화면에는 마지막에 받은 목록이 그대로 남아 멀쩡해 보인다. 그래서 이 버튼은 두 가지를
  한다 — 붙어 있으면 다시 달라고 하고, 끊겼으면 페이지를 새로 연다. 로비에는 잃을
  상태가 없으므로(이름은 브라우저에 남는다) 새로 여는 것이 가장 확실하다.
- `?room=코드` 직링크는 그대로 살아 있다. 방 안의 **링크 복사**로 만든다.

방을 고르면 그 주소로 **페이지를 옮긴다.** 소켓 하나로 방을 오가게 만들 수도 있지만,
접속할 때 방이 정해지는 지금 구조를 전부 뜯어야 해서 값이 맞지 않았다.

### 강퇴 — 방장 전권

사람 이름 옆의 ✕. 누르면 세 가지가 한꺼번에 일어난다.

1. 명단에서 **아예 지운다.** 이탈은 자리를 남겨두고 기다리지만 강퇴는 돌아올 자리를 없앤다.
2. 그 브라우저를 **그 방에 한해** 막는다.
3. 방을 **잠근다.** 내보내자마자 다시 들어오면 내보낸 의미가 없다. 방장이 언제든 푼다.

**계정이 없는 웹이라 새 창을 열면 뚫린다.** 막는 것이 아니라 돌아오는 값을 올리는 장치다.
그래서 `?seat=`(자리 가르기)는 **localhost에서만** 먹히게 막았다 — 바깥에서 열려 있으면
주소 한 글자로 새 자리를 만들 수 있어 강퇴가 그 자리에서 무의미해진다.

그리던 사람을 내보내면 **그 라운드는 접는다.** 반쯤 그린 그림을 조각내봐야 아무도 못 맞히고,
출제자 없는 라운드를 계속 돌릴 수도 없다.

### 입장 잠그기

방장이 새 사람만 막는다. **끊겼다 돌아오는 사람은 통과한다** — 잠금은 모르는 사람을
막자는 것이지 친구를 내쫓자는 것이 아니다.

### 제시어 210개 → 500개

테스터들이 50판 가까이 돌면서 **제시어를 실제로 바닥냈다.** 210개로는 한 판에 네 개씩
50판을 채울 수 없다. 스팀·앱으로 나갈 것을 생각하면 더 모자란다.

주제도 7개에서 **10개**로 늘렸다. 주제마다 정확히 50개다.

```
동물 · 음식 · 전자제품 · 직업 · 도구 · 자연 · 탈것 · 건물 · 전통문화 · 이야기 속 존재
```

- `물건`을 **생활용품**으로 좁히고, 거기 있던 자동차·비행기·배·기차·자전거를 **탈것**으로 옮겼다.
- **직업**, **행동**, **탈것**이 새로 생겼다.
- 파일 이름 앞에 번호를 붙였다. 주제 버튼 순서가 파일 이름 알파벳순이라 한글로 보면
  뒤죽박죽이었는데, 번호가 그 순서를 정한다.
- **같은 단어를 두 주제에 넣지 않는다.** 넣으면 그 단어의 주제가 뽑을 때마다 달라지고,
  맞히는 사람에게 주제는 힌트이므로 같은 그림에 다른 힌트가 붙는다. 테스트가 지킨다.

`목욕`(상황)과 `목욕탕`(장소), `치과의사`와 `의사`는 그림으로 구분이 안 돼서 뺐다.
`기차`⊂`제기차기`처럼 글자만 겹치는 것은 그림이 전혀 달라 그대로 뒀다.

**주제에 안 맞는 단어**도 걸렀다. 주제는 맞히는 사람에게 힌트이므로, 안 맞는 자리에
있으면 힌트가 오히려 방해가 된다.

- `인형`이 캐릭터에 있었다. 바비인형처럼 고유명이면 몰라도 인형 자체는 사물이다 —
  생활용품으로 옮기고 그 자리에 `골렘`을 넣었다.
- `공룡`도 같은 경우다. 티라노사우루스면 몰라도 공룡 자체는 생물 분류다 — `동물`로 옮겼다.
  자리를 내준 `무당벌레`는 점무늬 하나로 들켜서 조각 게임에 약했고, 덤으로
  `무당`⊂`무당벌레` 혼동도 사라졌다.

그러다 **주제 이름 자체가 문제**라는 것이 드러났다. `캐릭터`가 너무 넓어서 인형 같은 것이
슬쩍 들어올 수 있었다. **`이야기 속 존재`**로 바꿨다 — `존재`라서 사람이 아니어도 되고
(용·골렘·그리핀), `이야기 속`이라서 사물은 저절로 걸러진다.

**그리고 전체를 다시 훑었다.** 주제 다섯이 바뀌거나 새로 났다.

| 전 | 후 | 왜 |
|---|---|---|
| 생활용품 | **전자제품** | '생활용품'이 뭘 담는 자리인지 애매했다 |
| 장소 | **자연** | 생물 말고 환경으로 좁혔다 |
| 한국 | **전통문화** | 국가 상징(태극기·무궁화)보다 문화로 |
| 행동 | **도구** | 행동은 이름이 여럿이라 정확히 일치와 안 맞았다 |
| 상황 | **건물** | |

**음식에서 조리 안 된 것을 전부 뺐다.** 딸기·포도·당근은 음식이라기보다 과일·채소다.
빈 자리는 순대·불고기·냉면 같은 진짜 요리로 채웠다.

행동을 접은 이유가 하나 더 있다. `달리기`를 그렸는데 `뛰기`라고 답하면 오답이 된다 —
**정확히 일치로 채점하는 이 게임과 동작 이름은 궁합이 나빴다.**

**주제 사이에 겹치는 것**도 훑었다. 이 게임에서 겹침은 두 가지다.

- **글자가 포함되는 것** — `수영`⊂`수영복`처럼. 그림이 다르면 두고, 같으면 뺀다.
- **뜻이 품는 것** — `한옥`이 초가집·기와집을 품고 `한복`이 저고리·두루마기를 품는다.
  둘 다 그리면 같은 그림이 나오므로 **널리 쓰이는 쪽만 남겼다.**
  같은 이유로 `농악`과 `사물놀이`도 하나로 줄였다.

스포츠를 한 번 넣었다가 접었다. **그리면 결국 '사람이 뭔가 하는 그림'이라 직업과 구별이
안 됐다** — 축구는 골키퍼와, 체조는 발레리나와 같은 그림이 나온다. 대신 도구를 넣었다.
망치·톱·사다리처럼 실루엣이 또렷해서 이 게임에 훨씬 잘 맞고, 생활용품을 접으며 사라졌던
쓸모 있는 물건들이 제자리를 찾았다.

`일출`·`태양`도 `노을`과 한 덩어리라 노을만 남겼다. 해가 걸린 하늘은 셋 다 같은 그림이다.

### 주제를 여러 개 고른다

예전에는 **전체 아니면 하나**였다. 이제 원하는 만큼 골라 그 안에서만 나오게 할 수 있다.

`랜덤`을 **`전체`로 바꿔 맨 앞에 뒀다.** 뜻이 더 정확하고(라운드마다 바뀌는 것이 아니라
모든 주제가 후보라는 뜻이다), 기본값이 맨 앞에 있어야 처음 보는 사람이 안 헤맨다.

`전체`는 고른 것을 지우는 버튼이다. **아무것도 안 고른 상태가 곧 전체**이므로 따로
둘 필요가 없다 — 서버도 빈 배열 하나로 둘 다 표현한다.

### 제시어 기억이 방 단위가 됐다

**`한 판 더`를 누를 때마다 이미 나온 제시어 기억을 비우고 있었다.** 판 안에서만 안 겹쳤고,
이어서 여러 판을 돌리는 테스터들에게는 방금 나온 단어가 바로 다음 판에 되돌아왔다.
단어를 500개로 늘려도 이건 그대로였을 문제다.

이제 **방을 이어 쓰는 동안 계속 기억한다.** 고를 수 있는 단어를 다 쓰면 그때 비우고
처음부터 다시 돈다.

비울 때는 **지금 후보군의 단어만** 비운다. 주제를 고정해 두고 그 50개를 다 썼다고
나머지 아홉 주제의 기억까지 날리면, 나중에 랜덤으로 돌렸을 때 이미 나온 단어가
무더기로 되돌아온다.

남은 개수는 화면에 적지 않는다. 한번 적어봤다가 걷어냈다 — 플레이어가 알아서 할 수 있는
것이 아무것도 없는 숫자였다.

### 딴 데서 온 것 — 놀면서 동시에 고치기

친구들이 놀고 있는데 코드를 고치면 화면 서버가 **모두의 브라우저를 새로고침한다.**
안정된 판과 고치는 판을 포트를 갈라 따로 띄우면 서로 상관이 없어진다.

```bash
git worktree add ../PIZZART-play v1.3.0        # 같은 저장소를 다른 버전으로 한 벌 더
cd ../PIZZART-play && npm run play:all         # 5174 + 터널
cd ../PIZZART      && npm run dev              # 5173, 여기서 고친다
```

구조는 이미 맞았다. 클라이언트는 소켓 주소를 페이지 주소에서 끌어내므로 포트가 박혀
있지 않고, 게임 서버 포트와 vite의 `/ws` 프록시 대상은 둘 다 환경변수였다.
남은 것은 그 변수를 넘기는 방법뿐이었는데 `PORT=8081 …`은 cmd.exe에서 안 먹는다.
그래서 작은 실행기(`scripts/serve.mjs`)를 뒀다 — **의존성은 오히려 하나 줄었다**
(`concurrently`가 하던 일을 이 실행기가 한다).

**터미널도 하나로 줄었다.** `--tunnel`을 붙이면 터널까지 같이 뜨고, 친구에게 보낼
주소만 초록색으로 따로 크게 나온다. `Ctrl + C` 한 번이면 셋 다 내려간다.
터널이 없거나 끊겨도 판은 계속 돈다 — 놀다가 터널이 죽었다고 게임까지 멈추면 안 된다.

### 손잡이

없다. `content/rules.json`은 v1.3.0 그대로다.

### 고친 것

- **새로고침 한 번에 방이 사라지던 것.** 마지막 사람이 나가는 그 순간에 방을 지우고 있었는데,
  전원이 새로고침하면 잠깐 0명이 되는 순간이 있다. 그때 방 이름도 점수도 통째로 날아갔다.
  이제 **한동안 비어 있을 때만** 치운다.
- **이름 없이 '손님'으로 먼저 입장하던 것.** 이름 화면이 떠 있는데 이미 방에 들어가 있었다.
- **이름칸 줄이 인원 알약을 4px 파고들던 것.** 간격을 좁히려고 넣은 음수 여백 때문에
  입력칸 테두리와 알약 테두리가 겹쳐 보였다. 인원 줄에 아래 여백을 주는 쪽으로 바꿨다.
- **방 코드가 두 번 나오던 것.** 상단바의 `방 TBAX`와 방 표시줄의 `초코비의 방 · TBAX`.
  상단바 쪽을 없앴다 — 방 이름과 함께 있는 편이 알아보기 쉽다.
- **방 대기 화면에 `PIZZART` 제목과 소개문이 또 나오던 것.** 로비로 들어올 때 이미 읽은
  것이라, 방 안에서는 주제와 시작 버튼이 그만큼 아래로 밀리기만 했다.
  **게임 방법도 시작 버튼 아래로 내렸다** — 방에 들어오면 할 일이 먼저 보여야 한다.
- **인원 안내가 `참가자 4/4`이던 것.** 분수로 보이면 4가 정원처럼 읽혀서 다섯 명,
  여섯 명은 안 되는 줄 안다. 4는 하한이지 상한이 아니다.
  이제 **`참가자 6명 — 시작할 수 있습니다`**, 모자라면
  **`참가자 2명 — 최소 4명이 모여야 시작할 수 있습니다 (2명 더)`**.
- **이름칸이 셋이던 것.** 방 대기 화면에도 따로 있었다 — 로비가 생기기 전에는 거기가
  이름을 정하는 유일한 자리였기 때문인데, 같은 일을 하는 칸이 셋이면 한 곳을 고칠 때
  나머지를 잊는다. 이제 **로비로 들어올 때 하나, 방 안에 하나**뿐이다.
  방 안의 칸은 대기 중에는 누구나, 게임 중에는 도중에 들어온 사람에게만 열린다.

---

## v1.3.0 — 2026-08-25

**관전자와 새로 온 사람이 판에 제대로 앉게 만든 판**이다. v1.2에서 관전 자리를 열어놓고
그 사람들이 화면에서 겪는 것을 덜 봤는데, 플레이테스트에서 그게 한꺼번에 나왔다.
그 밖에는 **점수를 잃던 길 하나와 내가 만든 회귀 하나**를 잡았다.

### 새로 생긴 것

**제시어 바꾸기** — 라운드마다 2번 (`rules.json`의 `wordRerolls`).
"뭘 그릴지 모르겠다"로 판이 멈추는 것을 막는 장치다.

세 가지를 일부러 안 한다. **주제는 안 바꾼다** — 맞히는 사람들이 이미 주제를 받았으므로
바뀌면 그 힌트가 거짓이 된다. **시간은 다시 안 준다** — 돌려서 시간을 버는 길을 열면 안 된다.
**그리던 것은 지운다** — 다른 단어를 보고 그린 선이다.

원래 요청은 *제시어의 예시 그림을 보여달라*였는데, 210단어에 붙일 저작권이 깨끗한 그림을
구할 수 없어 접었다. 막막함의 원인이 "뭘 그릴지 모르겠다"였으므로 단어를 바꿔주는 쪽으로 풀었다.

**관전 중 화면 고르기** — 출제자 그림과 같이 낙서판을 버튼으로 오간다.
관전자를 낙서판에서 뺐던 것은 **그리는 것**을 막으려던 것이지 보는 것이 위험해서가 아니었다.
이미 정답을 아는 사람이라 봐서 새는 것이 없다. 보내주되 캔버스는 잠근다.

**도중에 들어온 사람의 이름** — 그 사람만 이름을 바꿀 수 있다.
계속 '손님'으로 남으면 결과 화면에서 누군지 알 수 없고, 반대로 원래 있던 사람이 도중에
이름을 갈면 그때까지 쌓인 답 기록과 이야기가 누구 것인지 어긋난다.

**방장 자동 이양** — 방장이 관전을 켜면 참여 중인 사람에게 넘어간다.
시작·다음 버튼은 방장만 누르는데, 그 버튼이 구경하러 온 사람 뒤에 잠기면 결과 화면마다
전원이 그 사람을 기다린다. 판을 안 하는 사람이 판을 진행시켜야 하는 자리가 된다.

넘어간 사람에게는 **작은 팝업**이 뜨고 왕관 알약이 잠깐 빛난다. 팝업은 몇 초 뒤 사라지므로
그 사이 딴 데를 보고 있었어도 알아채라고 둘을 같이 쓴다. **소리는 내지 않는다** —
통화를 켜고 하는 게임이라 남의 방에서 갑자기 소리가 나면 곤란하다.
팝업은 클릭을 통과시켜 아무것도 막지 않고, 움직임을 원치 않는 설정이면 움직이지 않는다.

**그림 모으기** — 라운드가 끝날 때마다 `data/drawings.jsonl`에 한 줄씩 쌓는다
(제시어·주제·획·맞힌 수). 나중에 *지난 그림 보기* 모드의 재료다.

지금까지는 **아무것도 모으고 있지 않았다.** 그림은 라운드가 끝나면 그냥 사라졌다.
커밋하지 않으므로 백업은 직접 해야 한다.

### 점수를 잃던 길을 막았다

**한글 조합 중 엔터.** 조합이 끝나기 전에 엔터가 먼저 온다. 그래서 **`고양이`를 치고
엔터를 눌렀는데 `고양`이 제출돼 오답으로 1점을 잃었다.**

제보는 "채팅에서 마지막 글자가 한 번 더 나온다"였는데(`다리다리` 뒤에 `리` 한 줄),
같은 원인이 답 입력칸에도 있었고 그쪽이 훨씬 비쌌다. 이름칸까지 세 곳을 함께 막았다.

### 고친 것

- **탭을 닫았다 들어와도 점수가 남는다.** 자리 식별자를 `sessionStorage`에 두어 탭을 닫는
  순간 새 사람이 됐다. `localStorage`로 옮겼다. 대신 같은 브라우저의 모든 탭이 한 자리를
  나눠 쓰므로, 혼자 여러 창으로 시험할 때는 `?seat=2`처럼 자리 이름을 붙인다.
  (예전 동작은 `test/multi-tab` 브랜치에 남겨뒀다.)
- **그리는 화면에 주제가 다시 나온다.** v1.2에서 상단바를 걷어내며 주제를 맞히는 화면으로만
  내렸고, 그리기 화면에 넣는 것을 빠뜨렸다. 출제자도 관전자도 주제를 볼 데가 없었다.
- **관전자가 점수판에서 완전히 빠진다.** 알약에서는 이미 뺐지만 결과 화면 점수줄과 최종
  등수에 0점으로 남아 꼴찌처럼 보였다. 결과 화면 답 목록이 그 점수줄로 그려지므로
  `(무응답)`까지 따라붙었다.
- **방장은 늘 참여 중인 사람에게 넘어간다.** 끊겨서 넘어가는 길도 마찬가지다. 예전에는
  명단 순서대로 골라 관전자가 방장이 될 수 있었다. 넘길 참여자가 없으면 관전자라도
  세운다 — 아무도 못 누르는 것보다 낫다.
- **이야기판이 라운드마다 맨 위로 올라가던 것.** 화면 전환이 `display:none`이라 그 사이
  브라우저가 스크롤 위치를 0으로 되돌린다. 줄이 쌓일수록 방금 나눈 말이 화면 밖으로
  밀려나 있었다. 결과·최종 화면을 띄운 **뒤에** 바닥으로 붙인다 — 숨겨진 동안에는
  높이가 전부 0이라 아무 데도 못 붙는다.

### 손잡이

`content/rules.json`에 `wordRerolls`(기본 2)가 생겼다.

### 아직 안 한 것

**같은 단어가 여러 판에 걸쳐 되풀이된다.** 이미 나온 단어 기억(`usedWords`)을 판이 끝날 때
지우기 때문이다. 주제를 고정해 두면 단어 풀이 30개로 줄어 두 판 만에 45% 확률로 겹친다.
로비에서 고른 주제가 초기화되지 않는 것도 함께 봐야 한다.

## v1.2.0 — 2026-08-21

관전 자리가 생겼고, 결과 화면에서 이야기를 나눌 수 있게 됐다.
그 밖에는 **플레이테스트에서 사람들이 실제로 걸려 넘어진 자리**를 고쳤다.

### 새로 생긴 것

**관전** — 그리지도 맞히지도 않고 출제자와 같은 것을 본다. 제시어, 그려지는 원본,
남들이 무슨 조각을 들고 헤매는지까지. 인원에도 출제 순번에도 들어가지 않는다.

로비에서만 고를 수 있다. 그 제약 하나가 나머지를 다 떠받친다 — 시작 전에 정하게 되고,
**게임 도중에 들어온 사람은 자동으로 관전자가 되어 판이 끝날 때까지 그대로다.**
예전에는 난입자가 다음 라운드에 슬쩍 합류해서, 남들이 한 판을 다 도는 동안
그 사람만 출제 없이 맞히기만 했다.

관전자는 낙서판에서 빠진다. 정답을 아는 사람이 대기 화면에 그리면 그게 곧 정답 유출이다.

**이야기** — 결과 화면과 최종 화면에서만 한마디씩 나눈다. 정답이 뜨는 그 1초가 이 게임에서
제일 재밌는 순간인데 나눌 데가 없었다.

회차 중에는 서버가 아예 받지 않는다. 그 자리에는 정답을 아는 사람이 셋 있고
(출제자·관전자·먼저 맞힌 사람), 그중 하나가 한 줄만 흘려도 나머지가 공짜로 점수를 가져간다.

로그는 한 판 내내 이어지고 라운드마다 구분선이 들어간다. 최종 화면이 그 판 전체의
반응 기록이 된다. 한 판 더를 누르면 점수와 함께 비워진다.

**출제자 점수** — 조립판에서 한 명이라도 맞히면 +5점. 여러 명이 맞혀도 5점이다.

점수가 조립판에서만 나오는 것이 핵심이다. 이 게임이 출제자에게 바라는 그림은
*조각으로는 못 알아보지만 합치면 알아보는* 그림인데, 그 조건이 그대로 점수 조건이 된다 —
조각만 보고 다들 일찍 맞혀버리면 라운드가 조립판에 닿지도 않아 0점이다.

**주제 넷** — 상황·한국·캐릭터·장소. 주제 7개, 단어 210개가 됐다.
원을 채우는 단어와 조각 하나로 안 들키는 단어를 골랐다.

**밝은 모드** — 고른 값은 브라우저에 남는다.

### 점수를 잃던 길을 막았다

**치는 것만으로는 제출되지 않는다.** 답이 250ms마다 자동으로 제출되고 있었다.
"고양"까지 치다가 회차가 끝나면 그게 오답 제출로 채점돼 1점이 깎였다 — 낼 생각도 없던 답인데.

**답을 낸 뒤에는 스킵을 못 누른다.** 스킵은 적어둔 답을 지운다(의도된 동작이다).
그래서 "답도 냈고 시간도 넘기자"고 누르면 낸 답이 조용히 사라졌다. 맞는 답이었어도.

**끊긴 사람의 기회를 뺏지 않는다.** 한 명이 잠깐 끊기면 그 회차가 전원에게 그 자리에서
끝나버렸다. 아직 아무것도 안 한 채 끊긴 사람은 기다려준다.

**기기 시계가 어긋난 사람만 혼자 시간이 빨리 가던 것**을 고쳤다. 서버 시각을 같이 보내
그 차이를 상쇄한다.

### 화면

**상단바를 걷어냈다.** 일곱 개가 전부 똑같은 알약이라 눈이 등급을 못 매겼고,
그래서 계속 봐야 하는 주제가 나머지 여섯에 묻혔다. 지금은 `PIZZART`와 아이콘 둘만 남았다.

- **주제를 조각 옆으로 내렸다.** 답을 치기 직전에 지나가는 자리라야 읽힌다.
- **시간 알약을 없앴다.** 같은 숫자가 회전 안내 원 안에 이미 크게 있다.
- **`연결됨`을 없앴다.** 늘 떠 있는 상태 표시는 신호가 될 수 없어서, 정작 끊겨도 눈에 안 들어왔다.
- **라운드는 점(`●●○○`)으로.** 읽는 것보다 세는 것이 빠르다.
- 방 코드는 로비에서만.

**낸 답만 지운다.** 회차가 넘어가도 쓰다 만 글자는 지키고, 이미 낸 답은 비운다.
틀린 답이 그대로 남아 있으면 무심코 또 내서 1점을 더 잃는다.
지운 답은 `지난 회차에 낸 답`으로 옮긴다.

**커서를 돌려준다.** 회차가 넘어가면 입력칸을 다시 클릭해야 했다.

**회전 안내 원 위에 부채꼴을 얹었다.** 숫자가 조각을 가려서 정작 조각이 안 보였다.

### 고친 것

- **낙서 색을 겹쳐 쓸 수 있다.** "남이 내 낙서를 지운다"는 제보로 한 번 잠갔다가 풀었다 —
  확인해 보니 지우기는 처음부터 색이 아니라 사람으로 갈리고 있었다. 없는 위험을 막느라
  아홉 색을 선착순으로 묶어둔 셈이었다.
- **스킵 집계가 회차 진행도로 바뀌었다.** 서버는 *답을 냈거나 스킵을 누른* 사람을 똑같이
  세는데 화면은 스킵만 세서, `2/3`인데 실제로는 `3/3`이고 회차가 그 자리에서 끝나는 일이 있었다.
  분모에서 빠진 사람 수(맞힘·관전)도 같이 적는다.
- **로비 인원이 관전자를 뺀다.** `참가자 5/4 — 시작할 수 있습니다`라고 해놓고 서버가 거절했다.
- **하트비트 15초 → 30초.** 회차(20초)마다 검사가 도는 셈이라 잠깐 렉이 걸린 사람이
  회차마다 끊겼다 붙었다 했다.

### 손잡이

`content/rules.json`에 `drawerScore`(기본 5)가 생겼다. `minPlayers`는 관전자를 빼고 센다.

---

## v1.1.0 — 2026-08-20

첫 플레이테스트에서 바로 나온 것들.

- **회차 시간을 회전 안내 원 안으로 옮겼다.** 위쪽 알약의 작은 글씨는 아무도 안 봤다.
  10초 아래로 내려가면 빨갛게 바뀌고 맥박이 뛴다.
- **낙서 색 고르기.** 기다리는 사람들이 같이 갈기는 판에서 누구 선인지 구분이 안 됐다.
- **출제자 현황판.** 그리고 나면 할 일이 없던 시간에, 남들이 지금 무슨 조각으로
  헤매고 있는지 보여준다. 사람마다 한 줄씩 — 합쳐서 한 판으로 보여주면 누가 무엇으로
  헤매는지가 사라진다.
- **회차별 답 기록.** 회차가 끝날 때만 쌓인다. 실시간으로 흘리면 통화 중인 출제자가
  반응해버려 정답이 샌다.
- **로비에서 주제 고르기** (방장만), **게임 방법 펼쳐 보기**.
- 결과 화면에서 다음 라운드로 넘기는 것을 방장이 직접 누른다.

---

## v1.0.0-beta — 2026-08-20

첫 플레이테스트 판. 게임이 성립하는 데까지.

- **원형 캔버스** — 원 밖에는 그릴 수 없다.
- **부채꼴로 자르고 전부 위를 향하게 돌린다.** 이 게임의 전부다. 서버는 섹터 번호를
  절대 안 보낸다 — 번호가 새면 "내 건 3시 방향"이 되고 게임이 그 자리에서 무너진다.
- **동시 공개, 정확 일치 판정.** 오타로 틀리는 것도 재미의 일부다.
- **회차마다 조각이 한 장씩 늘고 점수가 1점 깎인다.** 마지막 회차는 조각을 제자리에
  끼워 보여주는 조립판이다.
- **라운드 결과** — 조각이 회전이 풀리며 제자리로 날아가 붙는다.
- **들어옴·나감·다시 들어옴.** 새로고침해도 같은 자리로 돌아온다. 방장이 끊기면
  살아있는 사람에게 넘어간다.
- 4~9명. 진행 중에는 한 명 빠지는 것까지 버틴다.
