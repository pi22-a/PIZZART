# 구글 플레이 출시 체크리스트

> 상태: **2026-09-02.** 24시간 서버까지 끝났고 여기서부터가 스토어 일이다.

## 순서를 정하는 기준 — 14일 시계를 먼저 돌린다

개인 개발자 계정(2023-11-13 이후 생성)은 **테스터 12명이 14일 연속 참여**해야 프로덕션에
올릴 수 있다. 우회로가 없고 앱마다 시계가 새로 돈다.

**이게 유일하게 못 줄이는 시간이다.** 그러니 순서는 "시계를 최대한 빨리 돌리고, 그
14일 동안 나머지 서류와 자산을 만든다"가 된다. 스크린샷을 다 찍고 설명문을 다듬은
뒤에 테스트를 시작하면 그만큼 늦어질 뿐이다.

---

## 닫은 결정

- **도메인은 `pizzagame.app` 그대로 간다.** 이름을 PIZZART로 바꾼 뒤에도 유지하기로
  했다. 이유는 `RELEASE.md`의 도메인 절에 적었다. **다시 꺼내지 않는다.**
- ⚠️ 대신 **자동 갱신이 켜져 있는지 한 번 확인한다.** 출시 후 만료되면 설치된 앱이
  전부 깨진다. Cloudflare → 도메인 → Auto-renew

## 이미 끝난 것

- [x] **24시간 서버** — Lightsail(서울), 재부팅 자동 복구까지 확인
- [x] **고정 도메인 + HTTPS** — `pizzagame.app` (`.app`은 HTTPS 강제 TLD)
- [x] **PWA** — manifest, 서비스 워커, 아이콘 192/512/maskable
- [x] **개인정보처리방침** — `https://pizzagame.app/legal/privacy.html`
- [x] **릴리스 규율** — `main` = 프로덕션, 태그로 롤백 지점
- [x] **이름 확정** — PIZZART(피자르트)
- [x] **스크린샷 도구** — 들러리 봇으로 혼자 촬영 가능 (`npm run shots`)

---

## 1단계 — 시계를 돌리기 위한 최소한

### [ ] 1. Play 개발자 등록 ($25, 일회성)

https://play.google.com/console

개인 계정은 **신원 확인**(신분증)이 필요하고 며칠 걸릴 수 있다. **가장 먼저 시작한다.**
사업자로 등록하면 아래 12명 테스트가 면제되지만 서류가 더 필요하다.

### [ ] 2. 서명 키(keystore) 만들기

TWA를 만들려면 먼저 있어야 한다.

```bash
keytool -genkeypair -v -keystore pizzart-release.keystore \
  -alias pizzart -keyalg RSA -keysize 2048 -validity 10000
```

- **`../PIZZART-release/keys/`에 넣는다.** 저장소에 절대 넣지 않는다
- **잃어버리면 앱을 영영 업데이트할 수 없다.** 구글도 복구해 주지 않는다
- 만들자마자 이 컴퓨터 밖에 백업한다(비밀번호 관리자 등). 비밀번호도 같이

### [ ] 3. TWA 패키징

https://www.pwabuilder.com 에 `https://pizzagame.app` 을 넣으면 AAB를 만들어 준다.
(또는 `bubblewrap init --manifest https://pizzagame.app/manifest.webmanifest`)

- 서명은 2번에서 만든 키로
- 패키지 이름은 한 번 정하면 못 바꾼다 → `app.pizzagame.twa` 같은 형태

### [ ] 4. `assetlinks.json` 올리기 ← **지금 404다**

도메인이 이 앱을 인정한다는 증명서다. **없으면 앱 안에 주소창이 남는다.**

안에 들어가는 SHA-256 지문은 **2번 서명 키에서 나온다.** 그래서 키가 먼저다.
PWABuilder가 파일을 같이 만들어 준다.

```
https://pizzagame.app/.well-known/assetlinks.json
```

파일을 받으면 `public/.well-known/assetlinks.json` 에 넣고 배포하면 끝난다
(정적 서빙이 이미 그 경로를 처리한다).

### [ ] 5. 비공개 테스트 올리고 테스터 12명 모으기

여기서 **14일 시계가 시작된다.**

- "옵트인"은 초대를 수락하고 **실제로 설치**한 사람만 센다. 초대만 받은 사람은 안 센다
- 2026년부터는 테스터가 **실제로 써봤는지**도 본다
- 역설적으로 이 게임엔 유리하다 — 어차피 4~9명이 모여야 하는 파티 게임이라
  12명 모으기가 남들보다 쉽다

---

## 2단계 — 14일 기다리는 동안

### [ ] 스크린샷 촬영 (폰 기준 2~8장)

찍는 법과 여섯 화면 목록은 `../PIZZART-release/store/listing-ko.md` 참조.

```bash
npm run build && npm start          # 터미널 1
ROOM=SHOT npm run shots             # 터미널 2
```

**봇보다 먼저 들어가야 방장이 된다.** 컬러판으로 바꾸고, 게임 방법은 접고 찍는다.

### [ ] 대표 이미지(feature graphic) 1024×500

스토어 상단에 걸리는 가로 이미지. 필수다. 소재는 아이콘과 같은 **부채꼴로 잘린 원**.

### [ ] 스토어 등록 정보 다듬기

초안은 `../PIZZART-release/store/listing-ko.md` 에 있다. 앱 이름·짧은 설명·자세한 설명 완성.

### [ ] 콘텐츠 등급 설문

폭력·선정성은 없다. 다만 **사용자끼리 그림과 글을 주고받는다**는 점을 정확히 답한다
(이야기 기능, 낙서판). 빠뜨리면 나중에 등급이 재조정된다.

### [ ] 데이터 안전(Data safety) 양식

**개인정보처리방침과 어긋나면 반려된다.** 방침에 적은 것과 같게 답한다.

- 계정 없음, 광고·추적·분석 없음
- 수집: 사용자가 입력한 표시 이름(별명) — 방이 사는 동안만
- **그림이 서버에 쌓인다** — 누가 그렸는지는 안 남지만 반드시 밝힌다

### [ ] 테스터 피드백 반영

14일은 그냥 기다리는 시간이 아니다. 실제로 여럿이 도는 판에서 나오는 것을 고친다.

---

## 3단계 — 프로덕션

### [ ] 프로덕션 액세스 신청

세 부분짜리 신청서. 심사는 보통 7일 이하.

### [ ] 출시

---

## 출시 전에 생각해둘 것 (막는 것은 아님)

**서버를 재시작하면 돌던 판이 날아간다.** 방 상태가 메모리에만 있어서다. 지금은
"사람 없을 때 배포"로 넘기고 있는데, 모르는 사람이 쓰기 시작하면 그 규칙이 안 통한다.
방을 디스크에 남겼다가 되살리는 일은 꽤 큰 작업이라 출시 뒤로 미뤄도 된다.

**서버가 새벽에 죽으면 아무도 모른다.** systemd가 프로세스 재시작은 맡지만 인스턴스
자체가 멈추면 알 길이 없다. 무료 업타임 감시(UptimeRobot 등)를 걸어두면 충분하다.

**한 대뿐이다.** 방이 메모리에 있어 서버를 늘릴 수 없다. 512MB 한 대로 4~9명짜리 방
수십 개는 충분하지만, 그 이상 커지면 구조를 바꿔야 한다.
